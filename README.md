# SHACT

- `train.py` for training the model
- `inference.py` for testing the model
- `model.py` for the model architecture
- `data_modules.py` for loading and pre-processing the data
- `clustering_model.py` inference using sklearn AgglomerativeClustering
- `utils.py` utility functions
